const Scanner = ({ id, className }: { id: string; className: string }) => {
  // Scanner specific functionality goes here
  return <div id={id} className={className}></div>;
};

export default Scanner;
